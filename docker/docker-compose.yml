services:
  mongodb:
    image: mongo:latest 
    container_name: mongodb
    user: "${UID}:${GID}"
    ports:
      - "27017:27017"
    volumes:
      - ./mongodb:/data/db
    networks:
      - backend
    restart: always

networks:
  backend: